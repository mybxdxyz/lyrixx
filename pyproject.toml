[project]
name = "lyrixx"
version = "0.0.1"
dependencies = [
  "pyfiglet",
  "requests"
]

[build-system]
build-backend = 'setuptools.build_meta'
requires = [
    'setuptools',
]

[project.scripts]
lyrixx = "lyrixx.main:main"
